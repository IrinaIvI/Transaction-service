apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  namespace: test
spec:
  selector:
    app: transaction-service
  ports:
    - protocol: TCP
      port: 8002
      targetPort: 8002
